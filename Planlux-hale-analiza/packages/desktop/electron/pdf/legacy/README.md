# Legacy PDF (usunięte)

Stare pliki (`generatePdfPlanlux.ts`, `renderPlanluxTemplate.ts`) zostały usunięte.
Oficjalna ścieżka: `renderTemplate.ts` → `generatePdfFromTemplate.ts` → `generatePdf.ts` (runPrintToPdfFromFile), IPC `pdf:generate`. Template: `packages/desktop/assets/pdf-template/Planlux-PDF/`.
